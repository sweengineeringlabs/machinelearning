pub(crate) struct GatewayInput;

#[cfg(test)]
mod tests {
    use super::*;

    /// @covers: GatewayInput (struct instantiation)
    #[test]
    fn test_gateway_input_can_be_instantiated() {
        let _input = GatewayInput;
    }
}
