pub(crate) struct GatewayOutput;

#[cfg(test)]
mod tests {
    use super::*;

    /// @covers: GatewayOutput (struct instantiation)
    #[test]
    fn test_gateway_output_can_be_instantiated() {
        let _output = GatewayOutput;
    }
}
